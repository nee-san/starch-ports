pkgname=hash
pkgver=0
pkgrel=1
pkgdesc="Crypto-Hasher"
arch=('x86_64')
license=('Nil')
groups=('base')
makedepends=('libtomcrypt')
source=(http://strake.zanity.net:1104/hash/hash.c)

build() {
  cd $srcdir
  test -z $CC && export CC=cc
  $CC $CFLAGS -c -o hash.o hash.c
  $CC $LDFLAGS -o hash hash.o -ltomcrypt -lz $LDADD
}

package() {
  cd $srcdir
  install -dm 755 $pkgdir/bin
  install -m 555 hash $pkgdir/bin/
  for x in md_null md2 md4 md5 sha1 sha224 sha256 sha384 sha512 hmac ripemd; do
    ln $pkgdir/bin/hash $pkgdir/bin/hash.$x
  done
}

sha1sums=('6f52237f1332aeb0d7bbd81c69ee5aa02867512d')
